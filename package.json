{
  "name": "sharedgovernance",
  "private": true,
  "scripts": {
    "migrate-users": "node scripts/migrate-users-to-firebase.js",
    "delete-legacy-users": "node scripts/delete-sharedgovernance-local-users.js"
  },
  "devDependencies": {
    "firebase-admin": "^12.0.0"
  }
}
