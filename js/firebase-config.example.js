/**
 * Firebase configuration for shared submissions storage.
 * Copy to firebase-config.js and add your config from Firebase Console.
 * For GitHub Pages: add the contents of firebase-config.js as base64-encoded
 * secret FIREBASE_CONFIG in repo Settings → Secrets and variables → Actions.
 */
const FIREBASE_CONFIG = {
  apiKey: "your-api-key",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
